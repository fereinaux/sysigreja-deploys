@{
    Layout = "~/Views/Shared/_Inscricoes.cshtml";
}

<div class="middle-box inscricoes animated fadeInDown " style="height:550px;display:flex;margin:auto">
    <div class="row" style="margin:auto">
        <div class="col-sm-12">
            @if (ViewBag.Configuracao.LogoId != null)
            {
                <div class="row justify-content-center">
                    <div class="col-sm-offset-3 col-sm-6" style="display:flex">
                        <img style="max-width:100%;max-height:200px;margin:auto" alt="@ViewBag.Configuracao.Titulo" src="data:image/png;base64,@ViewBag.Configuracao.Logo" alt="Igreja Episcopal Carismática do Brasil" />
                    </div>

                </div>
            }
            <div class="row justify-content-center m-l-lg m-r-lg m-t-lg m-b-sm text-white text-center">
                <h3>"@ViewBag.Participante.Apelido"</h3>
                <h4>Obrigado por se inscrever no @ViewBag.Participante.Evento!</h4>
                <h4>Seu pagamento já foi confirmado e a sua vaga já esta garantida</h4>
                <h4>Nos vemos no dia @ViewBag.Participante.DataEvento! Até lá!</h4>
            </div>

            <div class="col-sm-12 p-h-sm text-white text-center">
                <h3>Qualquer dúvida entrar em contato com:</h3>
                <h3 style="cursor:pointer;margin-top:0" onclick="window.open(`https://api.whatsapp.com/send?phone=@ViewBag.Participante.PadrinhoFone`)">
                    @ViewBag.Participante.PadrinhoNome - @ViewBag.Participante.PadrinhoFone.Substring(3)
                </h3>
            </div>
        </div>

    </div>
</div>