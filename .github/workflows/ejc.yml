name: EJC
on:
  push:
    branches: [ejc]

jobs:
  deploys:
    runs-on: ubuntu-latest
    steps:
      - name: Trindade
        uses: johannes-huther/webhook.sh@v1.0.0
        with:
          webhook_url: "https://plesk6400.is.cc:8443/modules/git/public/web-hook.php?uuid=328c45f2-e65f-aea8-a8fd-baeacf358737"
          json: '{ "repository": "${{github.event.repository.full_name}}", "number": "${{github.event.number}}", "created_at": "${{github.event.pull_request.created_at}}", "updated_at": "${{github.event.pull_request.updated_at}}" }'
      - name: EJC Recon
        uses: johannes-huther/webhook.sh@v1.0.0
        with:
          webhook_url: "https://plesk6400.is.cc:8443/modules/git/public/web-hook.php?uuid=4632191a-70de-fb32-fad1-57897d7ad77c"
          json: '{ "repository": "${{github.event.repository.full_name}}", "number": "${{github.event.number}}", "created_at": "${{github.event.pull_request.created_at}}", "updated_at": "${{github.event.pull_request.updated_at}}" }'
      - name: Recon
        uses: johannes-huther/webhook.sh@v1.0.0
        with:
          webhook_url: "https://plesk6400.is.cc:8443/modules/git/public/web-hook.php?uuid=b2aa0709-303a-b350-586e-8910385f00a7"
          json: '{ "repository": "${{github.event.repository.full_name}}", "number": "${{github.event.number}}", "created_at": "${{github.event.pull_request.created_at}}", "updated_at": "${{github.event.pull_request.updated_at}}" }'
      - name: IecbEventos
        uses: johannes-huther/webhook.sh@v1.0.0
        with:
          webhook_url: "https://plesk6400.is.cc:8443/modules/git/public/web-hook.php?uuid=a3c1cf1d-51bc-b3b4-0849-9d63ad16798c"
          json: '{ "repository": "${{github.event.repository.full_name}}", "number": "${{github.event.number}}", "created_at": "${{github.event.pull_request.created_at}}", "updated_at": "${{github.event.pull_request.updated_at}}" }'
      - name: PDC
        uses: johannes-huther/webhook.sh@v1.0.0
        with:
          webhook_url: "https://plesk6400.is.cc:8443/modules/git/public/web-hook.php?uuid=e5a99bd6-8e2e-2ffe-b733-c90f91848c8e"
          json: '{ "repository": "${{github.event.repository.full_name}}", "number": "${{github.event.number}}", "created_at": "${{github.event.pull_request.created_at}}", "updated_at": "${{github.event.pull_request.updated_at}}" }'
      - name: Getsemani
        uses: johannes-huther/webhook.sh@v1.0.0
        with:
          webhook_url: "https://plesk6400.is.cc:8443/modules/git/public/web-hook.php?uuid=843290be-8694-27e8-3314-1639bfa03bd6"
          json: '{ "repository": "${{github.event.repository.full_name}}", "number": "${{github.event.number}}", "created_at": "${{github.event.pull_request.created_at}}", "updated_at": "${{github.event.pull_request.updated_at}}" }'
